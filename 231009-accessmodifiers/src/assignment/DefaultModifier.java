package assignment;

public class DefaultModifier {
    int x = 10;
    int y = 20;
    int z = x + y;

    void multiply() {
        System.out.println("Default Multiplication of " + x + ", " + y + " and " + z + " is " + (x * y * z));
    }
}
